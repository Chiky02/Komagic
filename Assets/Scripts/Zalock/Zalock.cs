using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Zalock : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        MovementMain xd = new MovementMain();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
